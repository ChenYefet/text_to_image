apiVersion: v1
kind: Secret
metadata:
  name: text-to-image-api-secrets
type: Opaque
# No sensitive configuration values are currently required by the service.
# This placeholder exists so that the Deployment's secretRef does not cause
# pod startup failures. Operators shall add sensitive values here as needed.
data: {}
